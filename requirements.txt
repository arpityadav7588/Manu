SpeechRecognition>=3.10.0
pyaudio>=0.2.13
faster-whisper>=1.0.0
soundfile>=0.12.1
pyttsx3>=2.90
psutil>=5.9.0
requests>=2.31.0
pyperclip>=1.8.2
opencv-python>=4.8.0
pillow>=10.0.0

# OPTIONAL FEATURES
# edge-tts>=6.1.9        (better TTS voice quality)
# pygame>=2.5.0           (required for edge-tts)
# pycaw>=20181227         (Windows precise volume control)
# openwakeword>=0.6.0     (always-on wake word, low CPU)
# chromadb>=0.4.0         (vector memory / RAG)
