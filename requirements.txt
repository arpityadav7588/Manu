# Core — must have
pyttsx3>=2.90
SpeechRecognition>=3.10.0
pyaudio>=0.2.14
psutil>=5.9.0
requests>=2.31.0

# Offline STT (strongly recommended — install these for offline mode)
faster-whisper>=1.0.0
soundfile>=0.12.1

# GUI
customtkinter>=5.2.0
pillow>=10.0.0

# Optional (install for extra features)
opencv-python>=4.8.0      # webcam security capture
pyperclip>=1.8.2          # clipboard read/write
screen-brightness-control>=0.16.0  # brightness control
pycaw>=20181227           # Windows precision volume (pip install pycaw)
comtypes>=1.2.0           # required by pycaw on Windows

# Phase 2 — Siri mode
pystray>=0.19.4
Pillow>=10.0.0
pygame>=2.5.0

# Phase 4 — Neural voice + vision
edge-tts>=6.1.9
pygame>=2.5.0
deepface>=0.0.79
opencv-python>=4.8.0
Pillow>=10.0.0

# Optional for Phase 4
# pytesseract>=0.3.10   # OCR fallback for screen reading
# pydub>=0.25.1          # MP3→WAV conversion for winsound fallback
